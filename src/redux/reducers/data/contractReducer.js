// import { CONTRACT_JSON } from "../../container/data";
import Token from '../../../ledger/build/contracts/Token.json';

export default (state=Token, action) => {
    switch (action.type) {
        default:
            return state;
    }
}